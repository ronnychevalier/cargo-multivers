[package]
name = "test-argv"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]

[profile.release]
strip = "symbols"
panic = "abort"

[package.metadata.multivers.x86_64]
cpus = ["generic", "alderlake", "skylake", "sandybridge", "ivybridge"]
